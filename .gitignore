node_modules
.next
out
.env
.env.*
!.env.example
.env*.local
.vercel
.railway
.direnv/
.cache/
.turbo/
.local
.envrc
.anchor
target
dist
!apps/drift/player-kit/dist/
!apps/drift/player-kit/dist/drift_vault.so
!apps/last-stop/native/dist/
!apps/last-stop/native/dist/last_stop_program.so
!apps/player-two/native/dist/
apps/player-two/native/dist/*
!apps/player-two/native/dist/player_two_program.so
coverage
test-ledger
npm-debug.log*
*.log
*.pid
*.seed
*.orig
*.swp
*.swo
*.tsbuildinfo
*.sqlite
*.sqlite-*
*.db
.secrets/
secrets.local/
private/
*.agekey
*.keystore
*.jks
.DS_Store
.vscode/
.idea/
.keys/
# THE CHAMBER's two keys are compiled into an already-deployed program and
# cannot be rotated without a redeploy, so they are tracked deliberately. Only
# descent is allowed here; apps/the-chamber/.gitignore names the exact files.
!apps/the-chamber/.keys/
*-keypair.json
