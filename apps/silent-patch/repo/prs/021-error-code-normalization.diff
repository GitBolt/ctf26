# PR 21: error code normalization

Status: merged

```diff
- return err!(ErrorCode::StrategyFailed);
+ return err!(ErrorCode::ExternalStrategyFailed);
```

Keeps keeper error classification consistent across strategy implementations.
