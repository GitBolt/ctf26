# PR 24: extract signer seed helper

Status: closed without merge

```diff
- let signer_seeds = &[b"vault-authority", vault_key.as_ref(), &[bump]];
+ let signer_seeds = vault_authority_seeds(&vault_key, bump);
```

Closed because the returned temporary made the CPI call site harder to audit. No deployed artifact was
built from this branch.
