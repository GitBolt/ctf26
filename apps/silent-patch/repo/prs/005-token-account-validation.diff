# PR 5: token account validation cleanup

Status: merged

```diff
- require_keys_eq!(reserve.mint, destination.mint, ErrorCode::MintMismatch);
+ require!(reserve.mint == destination.mint, ErrorCode::MintMismatch);
```

No behavior change. This aligns the error shape with the rest of the account validator.
