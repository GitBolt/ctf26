# PR 12: guard cache realloc

Status: merged

```diff
- cache.realloc(next_size, false)?;
+ require!(next_size <= Cache::MAX_SIZE, ErrorCode::CacheTooLarge);
+ cache.realloc(next_size, false)?;
```

Reviewer note: this account is keeper-owned and stores no authority or token balance.
