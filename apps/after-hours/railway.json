{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/after-hours/Dockerfile",
    "watchPatterns": ["apps/after-hours/**", "packages/participant-ticket/**", "packages/leaderboard/**", "packages/agent-integrity/**", "packages/request-budget/**"]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
